
<%= link_to t('.add'), new_product_path, class:"mx-2 text-teal-500 font-bold text-sm", data: {turbo_frame: "modal" } if Current.user %>
<%= link_to t('.favorites'), favorites_path,  class:"mx-2 text-teal-500 font-bold text-sm" if Current.user %>
<%= link_to t('.categories'), categories_url,  class:"mx-2 text-teal-500 font-bold text-sm" if Current.user&.admin?%>
<%= link_to t('.login'), new_session_path,  class:"mx-2 text-teal-500 font-bold text-sm" unless Current.user %>
<%= link_to t('.register'), new_user_path,  class:"mx-2 text-teal-500 font-bold text-sm" unless Current.user%>
<%= link_to t('.logout'), session_path(Current.user.id), data: {
    turbo_method: :delete,
    turbo_confirm: t('common.confirm')
}, class:"mx-2 text-teal-500 font-bold" if Current.user %>
